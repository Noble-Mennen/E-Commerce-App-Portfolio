const { Router } = require('express');

const router = Router();

// GET    /api/users/:id  — task 10
// PUT    /api/users/:id  — task 10
// DELETE /api/users/:id  — task 10

module.exports = router;
