const { Router } = require('express');

const router = Router();

// GET    /api/cart                    — task 11
// POST   /api/cart/items              — task 11
// PUT    /api/cart/items/:productId   — task 11
// DELETE /api/cart/items/:productId   — task 11
// DELETE /api/cart                    — task 11

module.exports = router;
