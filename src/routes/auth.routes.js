const { Router } = require('express');

const router = Router();

// POST /api/auth/register  — task 7
// POST /api/auth/login     — task 8
// POST /api/auth/logout    — task 8
// GET  /api/auth/me        — task 8

module.exports = router;
