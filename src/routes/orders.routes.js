const { Router } = require('express');

const router = Router();

// GET    /api/orders      — task 13
// GET    /api/orders/:id  — task 13
// PUT    /api/orders/:id  — task 13
// DELETE /api/orders/:id  — task 13

module.exports = router;
